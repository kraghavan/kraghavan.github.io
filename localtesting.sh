
cd ~/KarthikaRepo/AI/kraghavan.github.io

# install bundle
gem install bundler
bundle install

# Install dependencies (one time only)
bundle install

# Serve locally
bundle exec jekyll serve
# Check http://localhost:4000 — looks exactly like the live site